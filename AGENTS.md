# User Preferences & Rules

1. **Language Requirement**:
   - 모든 답변과 설명은 반드시 **한국어(Korean)**로 작성해야 한다.
   - 코드는 영문을 유지하되, 주석이나 설명은 한국어를 사용한다.

2. **Git 자동 푸시**:
   - 모든 작업이 끝나면 반드시 `git add`, `git commit`, `git push`까지 자동으로 실행한다.
